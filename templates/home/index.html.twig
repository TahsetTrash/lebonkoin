{% extends 'base.html.twig' %}

{% block title %}LaBonnePub{% endblock %}

{% block body %}
    <div class="jumbotron text-center text-white bg-primary">
        <h1>La Bonne Pub</h1>
        <h4>La Bonne Pub te permet de poster ta pub ici pour faire découvrir ton travail au monde !</h4>
    </div>
    <div class="jumbotron">
        <div class="container">
            {{ form_start(form) }}
            <div class="row" style="margin-left: auto; margin-right: auto;">
                <div class="col">

                    {{ form_row(form.field) }}
                </div>

                <div class="col">
                    {{ form_row(form.category) }}
                </div>
                <div class="col">
                    <button type="submit" class="btn btn-primary">Chercher une pub</button>
                </div>
            </div>

            {{ form_end(form) }}
        </div>
    </div>
    {% include 'ad/ads.html.twig' %}
{% endblock %}

